-- file: 4-first_table.sql
-- Description: Below script creates a table called first_table in the current database in my MySQL server

mysql -h localhost -u root -p -N -e "USE hbtn_0c_0; SHOW TABLES;"
